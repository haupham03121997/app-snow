export enum QueryKeys {
  CURRENT_USER = 'current-user',
  AUTH_COMBO = 'auth-combo',
  AUTH_CARD = 'auth-card',
  AUTH_MINING = 'auth-mining',
  AUTH_INVITE_FRIENDS = 'auth-invite-friends',
  AUTH_SYNC = 'auth-sync'
}
